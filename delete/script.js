const lowercase = abcdefghijklmnopqrstuvwxyz
const uppercase = abcdefghijklmnopqrstuvwxyz
const numbers = 0123456789;
const specialCharacters = !@#$%^&*()-=+_. 

promptCharacters("How Many Characters Would You Like?")
promptLowercase("Do you want lower case letters?")
promptUppercase("Do You Want uppercase letters?")
promptSpecialcharacters("Would you like to add a special character?")
promptNum("Do you want numbers?")



 for (let lowerCase = 0; lowerCase < array.length; lowerCase++) {
     const element = array[lowerCase];
     
 }

 for (let uppercase = 0; uppercase < array.length; uppercase++) {
     const element = array[uppercase];
     
 }

 for (let number = 0; number < array.length; number++) {
     const element = array[number];
     
 }

 for (let specialCharacters = 0; specialCharacters < array.length; specialCharacters++) {
     const element = array[specialCharacters];
     
 }
 